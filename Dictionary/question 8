a={}
n=eval(input("enter size of dictionary"))
for i inrange(0,n):
  x=input("enter key")
  y=input("enter value")
  a[x]=y
temp=[]
res=dict()
for key,val in a.items():
  if val not in temp:
    temp.append(val)
    res[key]=val
print(res)
